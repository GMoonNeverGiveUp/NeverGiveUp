# Use the public npm registry by default:
registry=https://registry.npmjs.org/

# But for our own @nebulae-scoped packages, hit Verdaccio:
@nebulae:registry=http://localhost:4873/

# Optional: allow direct IPFS installs via “ipfs:<CID>”
ipfs=https://ipfs.io/ipfs/
