// tsconfig.build.json
{
  "files": [],                // no files at the root
  "references": [
    { "path": "./src" },
    { "path": "./server" },
    { "path": "./scripts" }
  ],
  "compilerOptions": {
    "composite": true,
    "incremental": true,
    "noEmit": true,                       // we only emit in the subprojects
    "tsBuildInfoFile": "./dist/root.tsbuildinfo"
  }
}
