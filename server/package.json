{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo '✅ Backend build successful.'",
    "dev": "echo '🛰️ Starting backend server on http://localhost:4000...'",
    "lint": "echo '🔎 Linting server workspace...' && exit 0",
    "test": "echo '🧪 Running server tests...' && exit 0",
    "start": "node ./server.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
