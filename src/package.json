{
  "name": "galactic-genius-src",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 3000",
    "build": "astro build",
    "preview": "astro preview --port 3000",
    "lint": "eslint . --ext .js,.ts,.jsx,.tsx,.astro",
    "lint:fix": "eslint . --ext .js,.ts,.jsx,.tsx,.astro --fix",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run --coverage",
    "test:unit:watch": "vitest --watch",
    "test:e2e": "playwright install --with-deps && start-server-and-test \"pnpm dev\" http://localhost:3000 \"playwright test\"",
    "coverage": "vitest run --coverage",
    "coverage:html": "vitest run --coverage",
    "coverage:upload": "bash <(curl -s https://codecov.io/bash) -f coverage/lcov.info",
    "check": "pnpm lint && pnpm typecheck && pnpm test:unit",
    "check:all": "pnpm check && pnpm test:e2e"
  },
  "dependencies": {
    "astro": "^5.10.1",
    "@astrojs/react": "^3.6.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "@vitest/coverage-v8": "^3.2.4",
    "astro-eslint-parser": "^1.2.2",
    "coveralls": "^3.1.1",
    "eslint": "^9.29.0",
    "eslint-plugin-astro": "^1.3.1",
    "prettier": "^2.8.8",
    "start-server-and-test": "^2.0.12",
    "typescript": "^5.8.3",
    "ts-node": "^10.9.1",
    "vitest": "^3.2.4"
  },
  "eslintIgnore": [
    "node_modules/",
    "dist/",
    ".astro/",
    "**/*.d.ts",
    "public/"
  ],
  "eslintConfig": {
    "root": true,
    "parser": "astro-eslint-parser",
    "extends": [
      "eslint:recommended",
      "plugin:astro/recommended",
      "plugin:@typescript-eslint/recommended",
      "prettier"
    ],
    "parserOptions": {
      "ecmaVersion": "latest",
      "sourceType": "module",
      "project": "./tsconfig.json",
      "extraFileExtensions": [".astro"]
    },
    "rules": {
      "@typescript-eslint/no-unused-vars": [
        "warn",
        { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }
      ],
      "quotes": ["error", "single", { "avoidEscape": true }],
      "semi": ["error", "always"]
    },
    "overrides": [
      {
        "files": ["*.astro"],
        "rules": {
          "@typescript-eslint/no-unused-vars": "off"
        }
      }
    ]
  }
}
