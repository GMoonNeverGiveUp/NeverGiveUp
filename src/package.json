{
  "name": "src",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && echo '<html><body><h1>Galactic Genius</h1></body></html>' > dist/index.html",
    "dev": "echo '🌐 Starting frontend dev server at http://localhost:3000...'",
    "lint": "echo '🔍 Linting src workspace...' && exit 0",
    "test": "echo '🧪 Running unit tests for src...' && exit 0",
    "preview": "echo '📦 Previewing build output from dist/index.html...'"
  },
  "dependencies": {},
  "devDependencies": {}
}
