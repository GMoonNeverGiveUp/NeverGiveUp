import { defineConfig } from 'astro/config';

export default defineConfig({
  // Look for your pages/ and components/ directly under this folder,
  // instead of the default `src/`
  srcDir: '.',
});
